id: deploy-md
out: Deploy.md
sections:
  - release_gate
  - provider_route
  - preview_deploy
  - preview_validation
  - production_deploy
  - rollback_route
  - post_deploy_observability
  - final_audit_handoff
sequence:
  - confirm approved plan and deployment document exist
  - capture build, preview, production, smoke, and rollback commands
  - deploy preview first
  - validate preview before promotion
  - deploy production only after preview passes
  - record urls, revision, and smoke results
  - confirm rollback path before closing release
  - hand off to final audit before release signoff
